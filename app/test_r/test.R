# Obtener los números pasados como argumentos
# num1 <- as.numeric(commandArgs(trailingOnly = TRUE)[1])
# num2 <- as.numeric(commandArgs(trailingOnly = TRUE)[2])

num1 <- 2
num2 <- 4

# Calcular la suma
suma <- num1 + num2

# Mostrar el resultado
cat("La suma de", num1, "y", num2, "es:", suma, "\n")